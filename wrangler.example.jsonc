// Copy to `wrangler.jsonc` and fill in your own account-specific values.
// `wrangler.jsonc` is gitignored so ids and proxy hosts stay out of git.
{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "asset-tracker",
  "main": "src/index.ts",
  "compatibility_date": "2025-09-01",
  "observability": {
    "enabled": true,
    "logs": { "enabled": true, "invocation_logs": true, "head_sampling_rate": 1 },
    "traces": { "enabled": true, "head_sampling_rate": 1 }
  },
  "triggers": {
    // KRX run: KST Mon-Fri 20:30 = UTC Mon-Fri 11:30.
    // US run: KST Tue-Sat 07:00 (after the US regular close) = UTC Mon-Fri 22:00.
    // FX: KST Mon-Fri 12:00 (after Korea Eximbank publishes ~11:00) = UTC Mon-Fri 03:00.
    // Cloudflare weekdays: 1=Sunday ... 7=Saturday, so Mon-Fri is 2-6.
    "crons": ["30 11 * * 2-6", "0 22 * * 2-6", "0 3 * * 2-6"]
  },
  "vars": {
    "KIS_ENV": "prod",
    "KIS_BASE_URL": "https://openapi.koreainvestment.com:9443",
    "KIWOOM_ENV": "prod",
    // Caddy reverse-proxy vhost on your IP-whitelisted VPS (see deploy/caddy/).
    "KIWOOM_BASE_URL": "https://kiwoom.proxy.example.com"
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "asset-tracker-db",
      "database_id": "<d1-database-id>",
      "migrations_dir": "migrations"
    }
  ],
  "kv_namespaces": [
    {
      "binding": "CACHE",
      "id": "<kv-namespace-id>"
    }
  ]
}
